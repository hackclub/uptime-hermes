## Uptime hermes 
_named hermes because this bot correctly routes it to the correct teams_

Uptime hermes is a bot which is handling hackclub's uptime kuma notification and routing certain things to certain teams!

## Setup to repo

1. install nodejs! (node>=20)
2. install pnpm &  run `pnpm install`
3. populate `.env` with `.env.example`
4. run pnpm start!
